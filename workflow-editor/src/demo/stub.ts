/**
 * What `#demo` resolves to in a normal build — no stub, no fixtures, nothing
 * rendered. Keeps the demo code out of the bundle the API server serves.
 */
export const installDemoApi = () => { };

export const DemoBadge = () => null;

export const DEMO_ENABLED = false;
